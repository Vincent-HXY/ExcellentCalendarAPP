/**
 * Internal operations, audit, feature controls and dead-letter recovery.
 * Planned: it is never exposed through the public user API by default.
 */
@org.springframework.modulith.ApplicationModule
package com.excellentcalendar.cloud.admin;
