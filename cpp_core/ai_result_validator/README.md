# AI Result Validator

负责校验 AI 生成的候选结果。

## 具体任务

- 校验 AI 候选日程是否包含必要字段。
- 检查时间、提醒、分类、地点、标题是否可靠。
- 标记需要用户确认的低置信度字段。
- 阻止明显非法或危险的自动创建。

## 交付标准

- 不直接调用大模型。
- 不直接创建正式日程。
- 输出结构应包含置信度、问题列表和建议修复项。
- 不直接使用SQL语句，而是通过Storage Repository调用
