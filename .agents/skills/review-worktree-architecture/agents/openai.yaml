interface:
  display_name: "Worktree Architecture Review"
  short_description: "Review dirty changes, boundaries, scope, and completeness"
  default_prompt: "Use $review-worktree-architecture to review the uncommitted changes against the repository's current authoritative rules and verify them in proportion to risk."
