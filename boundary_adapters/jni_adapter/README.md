# JNI Adapter

负责 Kotlin 与 C++ Core 之间的函数调用和参数转换。

## 具体任务

- 定义 Kotlin 调用 C++ 的 native 接口。
- 转换字符串、时间、列表、对象、错误码等数据。
- 管理 native 对象生命周期和线程边界。
- 暴露 C++ Core 的稳定能力给 Android 层。

## 交付标准

- 参数转换必须可测试。
- 不在 JNI 层写领域规则。
- 明确处理空值、异常和编码问题。
