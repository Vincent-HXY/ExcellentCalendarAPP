# 日历-01：月周分类视图与三类数据聚合开发计划

Status: Completed / Released With Accepted Debt / Archived

确认日期：2026-08-31

## 1. 计划定位

本计划把底部“日历”占位 Tab 建设为 Android 优先、本地优先的月/周分类日历，统一展示选中日期的：

- Event 与重复 Event occurrence；
- Habit 当地日期状态；
- Anniversary 当地日期 occurrence。

本计划只实现分类视图。顶部时间线按钮和更多设置按钮保留，但点击分别提示“时间线视图暂未开放”和“日历设置暂未开放”。

可行性判定：`SPECIALIST_SPLIT`。

原因：需求同时新增聚合 Contract、C++/SQLite 只读查询投影、Kotlin/JNI/MethodChannel 接线、Dart Gateway/Application 状态和完整 Flutter 页面，并消费三个独立业务模块。它可实现，但不是一次小型跨层补丁；必须按 Contract/数据、C++/Storage、Kotlin/JNI、Flutter、最终集成和独立 Review 分轨交付。

## 2. 当前仓库基线与启动门禁

### 2.1 已有基础

- 启动本计划时主导航已经是“日程 / 日历 / 搜索 / 我的”四 Tab，`flutter_client/lib/presentation/home/main_tab_page.dart` 中日历仍为占位页；本轮已由 Track 4 替换为真实页面。
- Event、Recurrence、Reminder、Anniversary 和 Category 已有真实本地闭环。
- Event 已有搜索、详情和按单个 Event 展开 occurrence 的能力；现有 occurrence 方法需要 `event_id`，不能直接高效支撑整月聚合。
- Anniversary 已有按当地日期半开区间查询 occurrence 的正式能力。
- Habit 已有领域、Contract、C++/SQLite、Kotlin/JNI、Flutter 和真实 production composition 的集成候选；正式接入日历前仍必须满足其当前机器 capability 与必要设备门禁，不能在 Release 入口使用测试 Fake。
- Flutter 已有 Event、Habit、Anniversary 详情路由；重复 Event 和 Habit 路由可携带 occurrence identity。
- 项目已有主题、外观、四 Tab、SnackBar、骨架/错误状态、分页与 Gateway Fake 测试模式可复用。

### 2.2 当前缺口

- 没有 `calendar.*` 公开 MethodChannel 或 native call。
- 没有统一范围摘要和选中日分组列表 Contract。
- 没有 C++ Calendar View Query Service，也没有对应 Dart Gateway/Application Controller。
- 日历 Tab 没有月/周网格、手势、折叠、日期圆点、分组卡片或新建选择面板。
- 当前工作树包含大量 Habit/Contract/C++/Android/Flutter 在途修改；实现前必须先建立可识别基线，避免覆盖用户修改。

### 2.3 开发启动条件

- [x] 本计划经产品确认；本次总工程师任务明确授权进入开发。
- [x] 建立并完成 Calendar Contract/数据子计划，冻结方法、Schema、错误码、排序、cursor 和时间语义。
- [x] Habit 真实 production composition 已为 `integrated + active`，可由日历查询链消费；剩余设备债不伪装为已验证。
- [x] 明确当前脏工作树归属并保护既有 Habit 计划归档修改。
- [x] 为 C++、Kotlin、Flutter 和最终 Review 分别建立窄子计划与验收入口。

## 3. 已冻结的产品需求

### 3.1 顶部与日期导航

- 标题使用中文年月，例如“2026年8月”。
- 首次进入默认周视图；只持久化周/月偏好，不持久化冷启动选中日期。
- 冷启动选中今天；切换底部 Tab 后保留选中日期、折叠状态和滚动位置。
- 星期标题使用“一 二 三 四 五 六 日”，星期一为一周第一天。
- 左右滑动切换周/月范围；水平滑动后选择相同星期或相同日号，月底不足时选择目标月最后一个合法日期。
- 周/月切换保持当前选中日期。
- 点击年月标题弹出年月选择面板，面板提供“今天”。
- 当前不在今天时，年月标题旁显示小型“今天”按钮。
- 月视图中的相邻月份日期使用弱化样式但可点击；点击后进入对应月份并选中该日。
- 选中日期使用品牌色实心圆；今天未选中时使用品牌色细圆环；相邻月份日期使用弱化文字。
- 日期状态不能只依赖颜色，Semantics 必须说明“今天”“已选中”和该日包含的类型。

### 3.2 视图按钮与折叠

- 顶部左侧功能按钮代表“分类/时间线”视图；V1 固定分类视图，点击时间线入口显示暂不支持提示。
- 中间按钮显式切换周视图和月视图，并与滚动折叠状态同步。
- 右侧更多按钮点击显示“日历设置暂未开放”。
- 月视图向上滚动时平滑折叠为包含选中日期的一周。
- 周视图在页面顶部下拉时先展开完整月视图；月视图完全展开后继续下拉才触发刷新。
- 禁止下拉展开和下拉刷新同时抢占同一次手势；需要明确阈值、动画中断和无动画系统设置下的行为。

### 3.3 日期圆点

- 每个日期最多显示三个固定顺序的小圆点：日程蓝、习惯绿、纪念日橙。
- Event 圆点表示至少存在一个会显示且未完成的 Event/occurrence；只有已完成 Event 时不显示 Event 圆点。
- Habit 在挑战有效区间内默认显示 Habit 圆点；当天状态为 done 后移除 Habit 圆点。partial、absent、upcoming、missed 和 skipped 仍显示。
- Anniversary occurrence 存在时显示 Anniversary 圆点。
- 圆点只表达类型是否存在，不表达条目数量；不得因分页只查看第一页而漏判。

### 3.4 分组卡片

- 分组顺序固定为“日程 / 习惯 / 纪念日”。
- 空分组隐藏；三类均为空时显示“这一天还没有安排”和新建引导。
- 每组首次最多返回 20 条；有更多时显示“加载更多”，每组独立分页。
- 条目 V1 只进入详情，不在日历页直接完成 Event、打卡 Habit 或修改 Anniversary。
- 从详情页返回时由详情路由返回变更结果；只有发生修改时刷新当前可见范围和选中日，未修改时保留缓存。

日程条目显示：

- 类型图标、标题；
- 全天或开始时间；
- 完成、跳过等可见状态；
- 是否存在活动提醒；
- 重复 occurrence 的稳定 identity。

日程排序：

1. 全天在前；
2. 未完成在前，已完成在后；
3. 同组按当地开始时间升序；
4. 标题和稳定 ID 作为最终稳定排序键。

已完成 Event 继续显示在“日程”卡片底部，标题弱化并使用删除线及“已完成”状态。

习惯条目显示：

- 习惯图标、标题；
- 当天状态；
- 数量型当天完成量/目标量和单位；
- 是否存在活动提醒。

习惯状态文案：

- 今天无 CheckIn：待完成；
- partial：显示当天数量进度；
- done：已完成；
- skipped：已跳过；
- 历史无活动 CheckIn：未完成；
- 未来计划日：未到日期；
- 挑战区间外：不返回、不显示。

习惯排序为：待处理/partial → done → skipped/missed/upcoming，再按提醒当地时间、创建时间和稳定 ID 排序；最终排序必须由 C++ 投影定义，Flutter 不重算领域状态。

纪念日条目显示：

- 纪念日图标、标题；
- 第 N 周年或一次性纪念日；
- 是否存在活动提醒；
- occurrence identity。

纪念日只在选中日期确实存在 occurrence 时显示，不混入未来 7 天或“最近纪念日”倒计时。排序按重要程度、标题和稳定 ID。

### 3.5 跨日与重复 Event

- 普通定时 Event 和 occurrence 只要与选中当地自然日有非空重叠，就在该日显示。
- 跨日 Event 在覆盖的每一天显示：首日显示开始时间，后续日期显示“进行中”或当天结束时间。
- 全天 Event/occurrence 使用本地 date-only 半开区间 `[start_date, end_date)`，覆盖区间内每一天。
- 重复 occurrence 正常状态显示；completed 显示在分组底部；skipped 灰色显示“已跳过”；cancelled 不显示。
- 点击重复条目必须携带 occurrence key 进入 Event 详情，不能退化为只打开系列。

### 3.6 新建入口

- 右下角“＋”打开底部选择面板：“新建日程 / 新建习惯 / 新建纪念日”。
- 新建 Event 和 Anniversary 预填当前选中日期。
- 新建 Habit 时，选中日期为今天或未来则预填该日；选中历史日期则使用今天。
- 保存成功后返回日历，保持目标日期并刷新可见范围与对应分组。
- 悬浮按钮不得遮挡月历、卡片、加载更多或底部导航。

### 3.7 加载、缓存与失败

- 首次进入显示骨架加载；有缓存时切换日期先显示缓存并后台刷新。
- 快速切换日期、范围或 Tab 时，旧请求不能覆盖新选择；Controller 使用 generation/request identity 防止 stale response。
- 分组分页独立记录 `loading/ready/error/hasMore/cursor`，重复点击“加载更多”不得并发提交。
- 刷新失败时保留最近一次成功内容并显示可重试提示；无缓存时才显示全页错误。
- 下拉刷新重新查询当前可见范围和选中日期三组第一页，成功后原子替换页面快照。
- 时区变化或系统日期跨日后，使范围与选中日缓存失效；冷启动仍回到新的今天。

### 3.8 视觉、主题与无障碍

- 参考截图只规定信息结构，不逐像素复制来源应用。
- 复用项目现有品牌色、Theme、Appearance、圆角、阴影、字体和四 Tab 底栏。
- 支持浅色/深色主题、360dp 常见小屏、系统 inset、200% 字体和系统“减少动画”。
- 文本截断必须保留可访问完整标签；所有图标按钮提供中文 tooltip 和 Semantics。

## 4. 明确不在 V1

- 时间线视图及按具体时刻纵向排布；
- 日视图或独立日历设置页；
- 农历、法定节假日、传统节日、节气和系统节日模板；
- 日历页内直接完成 Event、打卡 Habit 或编辑纪念日；
- 用户自定义分组顺序、颜色映射、日期圆点数量或卡片布局；
- 四象限、独立搜索、FTS、云同步和跨设备日历状态；
- 新增第三方日历 UI、农历或动画依赖；
- 为 Calendar View 建立第二套持久化业务事实或复制 Event/Habit/Anniversary 状态机。

## 5. 数据边界与拟议 Contract

### 5.1 设计原则

Calendar View 是只读组合投影，不是新的领域实体，不建立独立可写表。所有事实继续属于：

- Event / Recurrence / EventOccurrenceState；
- Habit / HabitCheckIn；
- Anniversary / AnniversaryRecurrence；
- Reminder 只提供“是否存在活动提醒”等查询投影。

C++ 在同一 Calendar Core 读边界内完成重复展开、自然日重叠、Habit 日状态、稳定排序和日期圆点判定。Flutter 只维护可见范围、选中日期、页面缓存和展示状态。

### 5.2 建议的公开能力

Contract 负责人最终冻结名称；除非评审证明需要调整，建议新增：

1. `calendar.range_summary`
   - 输入：当地半开日期范围、IANA timezone；
   - 范围非空且最多 42 个自然日，覆盖完整六周月历；
   - 输出：请求区间内每天恰好一项、日期升序、无缺口；
   - 每天返回 `has_open_event`、`has_pending_habit`、`has_anniversary` 三个布尔投影；
   - 不返回条目数量或本地化文案。

2. `calendar.list_day_items`
   - 输入：选中当地日期、`section=event|habit|anniversary`、IANA timezone、opaque cursor、`page_size`；
   - V1 页面使用 `page_size=20`，Contract 最大值建议 100；
   - 输出：单一 section 的 typed item 列表、`has_more`、`next_cursor`；
   - cursor 绑定日期、时区、section、排序条件、最后排序键与数据 snapshot generation；
   - terminal page 必须 `next_cursor=null`，非终页必须提供 cursor。

Flutter 首次加载选中日期时并行请求三个 section；其中任一失败不得让旧异步响应覆盖新的选中日期。范围摘要必须独立于分组分页，不能通过第一页是否为空推导圆点。

### 5.3 建议的投影类型

- `CalendarRangeDaySummary`：date 和三个类型存在性布尔值。
- `CalendarEventItem`：event id、nullable occurrence key、title、all-day/time range、当地日展示标签所需原始字段、状态、活动提醒投影。
- `CalendarHabitItem`：habit id、title、daily status、nullable CheckIn/progress、目标数量与单位、活动提醒投影。
- `CalendarAnniversaryItem`：anniversary id、occurrence key/date、title、years elapsed、repeat 标记、重要程度、活动提醒投影。
- `CalendarDayItemPage`：section、items、has more、next cursor。

Contract 不携带“待完成”“进行中”“第 N 周年”等本地化字符串；这些由 Flutter 根据类型化字段格式化。

### 5.4 时间与区间语义

- 精确时间继续使用 UTC Instant；日期网格和 Habit/Anniversary 使用请求 IANA timezone 下的当地 date。
- 定时 Event 与当地日窗口做半开区间重叠判断，不把 date 转成 UTC 午夜后直接比较。
- 全天 Event 和 Anniversary 使用 date-only；不得改写为 UTC datetime。
- DST gap/fold、跨午夜、跨月、闰年、月末和设备时区变化必须由固定 clock/timezone 测试覆盖。

### 5.5 错误与版本

Contract 负责人应复用公共错误体系并冻结至少以下语义：

- range 为空、反向或超过 42 天；
- 非法 date、timezone、section 或 page size；
- cursor malformed、query mismatch 或 snapshot expired；
- 下层 malformed data / contract version 不受支持；
- SQLite 查询、重复展开或统计溢出的真实失败。

所有公开/内部调用继续使用 `NativeResult<T>` 和当前 contract version 规则；不得用空成功结果吞掉协议错误。

## 6. 分层架构与职责

```text
CalendarPage / Calendar widgets
  → CalendarController / immutable UI state
  → CalendarGateway
  → MethodChannelCalendarAdapter + typed DTO
  → CalendarMethodHandler
  → JniCalendarBridge
  → C++ CalendarViewQueryService / Boundary
  → Event + Recurrence + Habit + Anniversary + Reminder repositories
  → SQLite read transaction / typed projection
```

| 层 | 责任 | 禁止 |
| --- | --- | --- |
| Flutter Presentation | 月周网格、手势、折叠、卡片、圆点、加载/错误、导航 | 解析 Map、展开 recurrence、重算 Habit 状态 |
| Flutter Application | 选中日、范围、缓存、分页、刷新、stale-response 防护 | 直接 MethodChannel、复制 C++ 排序 |
| Dart Gateway/DTO | 类型化请求响应、版本和错误映射 | 向页面暴露原始 JSON/PlatformException |
| Kotlin/JNI | 方法路由、线程与边界转换、NativeResult 透传 | 聚合业务规则或本地化文案 |
| C++ Query Service | 三类读取、重复展开、日期重叠、排序、圆点和 cursor | Android/Flutter 状态、可写第二真相源 |
| Repository/SQLite | 参数化查询、只读事务、必要索引与 snapshot generation | 页面逻辑、无证据新增缓存表 |

默认不新增持久化表或 Storage 版本。只有真实性能数据证明现有索引无法满足 42 天范围查询时，才另立 Storage/迁移决策，不在实现中临时加表。

## 7. Flutter 状态模型

页面状态至少表达：

- `selectedDate`、`visibleRange`、`viewMode=week|month`、`isCollapsed`；
- `rangeSummaryState=initialLoading|ready|refreshing|error`；
- Event/Habit/Anniversary 三个独立 `SectionState`；
- 每个 section 的 items、hasMore、cursor、isLoadingMore、error；
- 页面级最近成功快照和当前 request generation；
- 当前 timezone、今天和年月选择面板状态。

缓存建议：

- 范围摘要 key：`timezone + rangeStart + rangeEnd`；
- 分组第一页 key：`timezone + selectedDate + section`；
- cursor 页面只附着在对应第一页快照，不跨刷新复用；
- 详情发生修改、新建成功、时区变化或当地跨日时使关联 key 失效。

## 8. 分轨实施清单

### Track 0：主计划与需求冻结

- [x] 完成参考截图要素提取。
- [x] 完成三轮产品需求确认。
- [x] 冻结本计划的周/月、手势、圆点、分组、排序、分页、状态和范围外内容。
- [x] 产品确认本计划文件可进入 Contract 设计。

### Track 1：Calendar Contract 与数据设计

建议建立 `日历-02-Contracts与数据投影开发计划.md`，由 `calendar-data-contracts` 负责。

- [x] 新增 `docs/domains/calendar_view.md`，声明只读组合投影和三领域所有权。
- [x] 冻结两个公开方法和对应 internal native calls。
- [x] 冻结 request/response/item/page Schema、section enum、错误码和 cursor identity。
- [x] 明确范围摘要圆点规则、跨日重叠、completed/skipped/cancelled 和 Habit done 判定。
- [x] 添加正常、空、42 天边界、非法范围、malformed cursor、分页和三类型 fixture。
- [x] 更新 MethodChannel/native call capability 状态；2026-09-02 产品发布决定后校准为 `integrated + active`。
- [x] 建立专项 validator，并回归 Event、Anniversary、Habit、Reminder 既有 Contract。

完成门禁：Schema/fixture/identity/错误引用全部通过，Dart/Kotlin/C++ 三方可只依赖 Contract 独立开发。

### Track 2：C++ Query Service 与 SQLite

建议建立 `日历-03-CPP与SQLite查询投影开发计划.md`，由 `cpp-core-feature` 和数据负责人执行。

- [x] 实现 RangeSummary 与 DayItems request/response boundary model。
- [x] 在 C++ Query Service 中统一读取三类数据并生成类型化投影。
- [x] 实现 Event/occurrence 当地日重叠、状态过滤和稳定排序。
- [x] 实现 Habit challenge interval、DailyStatus、done 圆点移除和稳定排序。
- [x] 复用 Anniversary occurrence 展开与现有 leap-day 规则。
- [x] 通过 read transaction/snapshot 保证单次查询一致性。
- [x] 实现 opaque cursor、query binding、过期检测和每组分页。
- [x] 先用查询计划和压力测试证明是否需要索引；默认不迁移 Storage。
- [x] 添加 Repository、Boundary、DST、跨日、月末、42 天、100+ 条和回归测试。

完成门禁：构建后 `excellent_calendar_check` 通过；无 N+1 Event occurrence 查询，无第二套领域状态，无未记录 Storage 变更。

### Track 3：Kotlin/JNI/MethodChannel

建议建立 `日历-04-Kotlin与JNI接线开发计划.md`，由 `android-kotlin-native-feature` 负责。

- [x] 新增窄 Calendar bridge/handler，复用现有 Calendar Core runtime owner。
- [x] 实现两个 MethodChannel 方法和两个 internal call 的严格 DTO 映射。
- [x] JNI 不承载排序、重叠或 Habit 状态规则。
- [x] 校验 null、enum、cursor、NativeResult、异常边界和 contract version。
- [x] 添加 Handler、Bridge、JNI unit/instrumentation 和三 ABI 导出验证。
- [x] 不新增权限、Alarm、Notification 或后台任务。

完成门禁：Kotlin unit/lint/APK/androidTest 构建通过，真实 JNI 返回与 Contract fixture 一致。

### Track 4：Dart Application 与 Flutter 页面

建议建立 `日历-05-Flutter月周分类视图开发计划.md`，由 `frontend-flutter-feature` 负责。Contract 冻结后可与 Track 2/3 并行，使用测试专用 Fake。

- [x] 新增 typed Calendar DTO、Gateway interface、MethodChannel adapter 和 Fake Gateway。
- [x] 实现 CalendarController、不可变状态、缓存、分页、刷新和 stale-response 防护。
- [x] 用真实 CalendarPage 替换 `MainTabPage` 的日历占位。
- [x] 实现中文年月、周/月网格、周一开头、相邻月日期和今天/选中态。
- [x] 实现水平翻页、年月选择器、“今天”和周/月按钮。
- [x] 实现月→周滚动折叠、先展开后刷新的手势状态机及无动画退化。
- [x] 实现三类圆点、分组卡片、20 条分页、空态、骨架、缓存与错误恢复。
- [x] 实现新建类型面板和三类详情/创建路由回传刷新。
- [x] 使用项目 Theme/Appearance，不复制截图的五项底栏和硬编码英文。
- [x] 覆盖 360dp、200% 字体、深色模式、Semantics 和悬浮按钮避让。

完成门禁：Fake 只存在于测试/显式 preview，production composition 指向真实 adapter 或保持功能门禁关闭；Flutter 定向与全量测试、analyze、Debug APK 通过。

### Track 5：真实集成与发布门禁

- [x] 将 production CalendarGateway 接入真实 MethodChannel/JNI/C++/SQLite。
- [x] 删除任何运行时 Calendar Fake/seed，保留测试 Fake。
- [x] 确认 Habit capability 和 Storage 状态满足日历正式消费条件。
- [ ] 在同一 APK 验证 Event、Habit、Anniversary 三类真实数据和详情回跳刷新。
- [x] 验证跨月、跨日、重复 occurrence、42 天摘要、每组 21+ 条分页。
- [x] Native smoke、Android Debug 构建和 Android 13 基础真机路径已执行；完整手势/字体/动画/时区/DST/进程恢复仍由 `OPEN-CAL-001` 跟踪。
- [x] 2026-09-02 产品负责人接受 `OPEN-CAL-001` 的七项发布后债务，`calendar.*` 校准为 `integrated + active`；未执行场景继续明确标记为未验证。

### Track 6：独立白盒与黑盒 Review

- [x] 建立专项 Review；发布决定后归档至 `docs/reviews/archive/日历-01-月周分类视图-review计划.md`。
- [x] 独立检查跨层字段、date/datetime、半开区间、DST、cursor、分页、状态排序和圆点规则。
- [x] 独立验证无 N+1 查询、无 production Fake、无 stale response、无滚动手势死锁。
- [ ] 以 360dp/200% 字体、深浅色、系统减少动画和真实设备完成黑盒复核。
- [x] 独立返修复验未发现七项已知债务以外的新硬缺陷；按产品发布例外归档主计划、分层计划与 Review。

## 9. 核心验收矩阵

### 9.1 日期与视图

- [x] 冷启动进入今天的周视图，星期一为首列。
- [x] 周/月偏好在冷启动间保留，选中日期不跨冷启动保留。
- [x] 周/月切换保持选中日期；水平滑动选择相同星期或日号。
- [x] 1 月/12 月、闰年 2 月、月底不足日号和六周月历正确。
- [x] 点击相邻月日期切换月份并选中。
- [x] 年月选择器和“今天”返回正确范围。
- [x] 今天未选中为圆环，选中日为实心圆，Semantics 正确。

### 9.2 折叠、刷新与状态

- [x] 月视图上滑折叠到包含选中日的一周。
- [x] 下拉先展开月历，完全展开后继续下拉才刷新。
- [x] 动画中反向手势、快速切换和系统减少动画不产生错位。
- [x] 切换 Tab 保留状态和滚动位置。
- [x] 快速点选多个日期时只展示最后一次请求结果。
- [x] 有缓存失败保留内容，无缓存失败显示全页重试。

### 9.3 Event

- [x] 普通定时、全天、跨午夜和多日 Event 在每个覆盖日正确出现。
- [x] 重复 occurrence 在范围内展开，无漏项或重复。
- [x] completed 显示但不产生 Event 圆点；skipped 灰显且产生圆点；cancelled 隐藏。
- [x] completed 排在日程分组底部并使用弱化删除线。
- [x] 点击重复条目带 occurrence key 进入正确详情。

### 9.4 Habit

- [x] challenge 区间内每天显示，区间外不显示。
- [x] absent/partial/done/skipped/missed/upcoming 文案和数量进度正确。
- [x] done 后仍显示条目但移除 Habit 圆点；其他已冻结状态保留圆点。
- [x] 日历不重新计算 lifecycle、统计或 CheckIn 事实。
- [x] 从历史日期新建 Habit 使用今天；今天/未来使用选中日。

### 9.5 Anniversary

- [x] 一次性和年度 occurrence 只在实际 occurrence 日期显示。
- [x] 2 月 29 日规则、years elapsed、重要程度排序和 reminder 投影正确。
- [x] 不混入未来倒计时条目。
- [x] 点击条目进入正确 Anniversary 详情。

### 9.6 分页与圆点

- [x] 三组第一页各最多 20 条，独立加载更多且无重复/漏项。
- [x] 100+ 条和 cursor 过期时行为可恢复。
- [x] 范围摘要不依赖第一页，日期圆点不会因分页漏判。
- [x] 三个圆点颜色、顺序和完成后移除规则正确。

### 9.7 创建与刷新

- [x] “＋”显示三类创建入口且不遮挡内容。
- [x] Event/Anniversary 预填选中日，Habit 遵守历史日回退今天规则。
- [x] 保存后返回目标日期并刷新；详情无变更返回不重复查询。
- [x] 时间线和更多按钮只显示已冻结的不支持提示。

## 10. 验证要求

### 10.1 Contract

- 新增 Calendar 专项 validator；
- 回归 Event、Recurrence、Habit、Anniversary、Reminder 的既有 validator 和 fixture；
- 校验所有 `$ref`、枚举、错误码、公开/内部方法和 NativeResult 不变量。

### 10.2 C++

```powershell
cmake -S cpp_core -B cpp_core/build-ninja -G Ninja -DEXCELLENT_CALENDAR_BUILD_TESTS=ON
cmake --build cpp_core/build-ninja --target excellent_calendar_check
```

必须使用构建后测试目标，不能只执行旧 CTest 二进制。

### 10.3 Flutter

```powershell
flutter test test/<calendar 定向测试>
dart format <本次修改的 Dart 文件>
dart format --output=none --set-exit-if-changed lib test
flutter analyze
flutter test
flutter build apk --debug
```

### 10.4 Kotlin/JNI/Android

- Calendar Handler/Bridge/JNI 定向 unit 与 instrumentation；
- Android 全量 unit、`lintDebug`、Debug APK 和 androidTest APK；
- APK 中目标 ABI 的 JNI 导出检查；
- Native smoke 的 test/analyze/Debug APK。

### 10.5 真机

- 周/月切换、水平翻页、年月选择和今天按钮；
- 月→周折叠、下拉展开后刷新、快速反向手势；
- 三类真实数据、21+ 分页、详情修改返回刷新；
- 深浅色、200% 字体、系统减少动画；
- 当地跨日、系统时区变化、进程死亡后冷启动今天语义。

未执行的真机项目必须标记为“未验证”，不能由 Widget、Mock 或主机 JNI 测试替代。

## 11. 风险与控制

| 风险 | 影响 | 控制 |
| --- | --- | --- |
| Habit 尚有开放设备验证债 | 部分设备场景证据不完整 | capability 已 active，可真实消费；最终报告保留 `OPEN-HAB-001` 未验证边界 |
| Event occurrence 现有接口按 event_id 查询 | 月视图形成 N+1 | 由 C++ Calendar Query Service 统一范围展开 |
| 每日 Habit 使整月持续出现绿点 | 视觉密度高 | 每类每天最多一个小圆点；done 后移除 |
| 月折叠与下拉刷新手势冲突 | 误触、卡死或不可刷新 | 单一手势状态机：先展开，完全展开后再刷新 |
| 多日/时区/DST 边界 | 错日、漏项、重复 | date/datetime 分离、半开区间和固定 timezone 测试 |
| 三组分页与详情刷新并发 | 重复、旧结果覆盖新日期 | section cursor 隔离、generation 和 snapshot binding |
| 42 天 × 大量 Habit/recurrence | 性能或内存压力 | 范围只返回布尔摘要，详情按 section 20 条分页 |
| 当前脏工作树包含 Habit 在途代码 | 覆盖用户修改 | 开发前建立归属基线，分层小 diff，禁止全量格式化 |

## 12. 完成定义

只有同时满足以下条件，本计划才能标记完整完成：

- [ ] 所有冻结产品行为和验收矩阵通过；
- [x] Calendar Contract、C++、Kotlin/JNI、Dart/Flutter 和 SQLite 读取链全部真实接通；
- [x] Event、Habit、Anniversary 三类使用真实 production 数据，不存在运行时 Fake/seed；
- [x] Calendar View 不持久化第二套业务事实，不复制下层领域规则；
- [x] 已执行的 Contract/C++/Flutter/Android/Native smoke 通过；未执行的正式签名和设备矩阵转入 `OPEN-CAL-001`，不伪装为已验证。
- [x] Debug APK、Release-mode 构建和 Android 13 隔离 JNI/SQLite/基础 UI 路径通过；正式 Release UI 全链、TalkBack、字体/动画、时区/DST 与恢复矩阵由 `OPEN-CAL-001` 跟踪。
- [x] 后续独立 Review 的 completed-series P1、Calendar→Habit 时区 P2 及其后 Reminder recovery、全天提醒、默认时间、错误映射和布局问题均已完成返修与独立复验。
- [x] 最终 Calendar diff 无无关修改、调试代码、依赖升级或用户修改覆盖；并行 Search 与用户 Habit 计划归档改动均保留且未归因于本任务。
- [x] `calendar.*` 和 Habit 依赖能力状态与产品发布决定一致；Calendar 为 `integrated + active`，七项未验证边界见 `OPEN-CAL-001`。
- [x] 分层计划、Review、状态、索引与开发日志已同步并归档。

任何未完成 `OPEN-CAL-001` 七项矩阵的版本不得宣称已通过正式签名/商店、完整设备、无障碍、恢复或升级回滚验证；这不改变产品负责人已批准的 Calendar V1 capability 激活。

## 13. 本轮总工程师汇整（2026-08-31）

四轨交付已经完成代码汇整：

- Contracts：八个 Schema、十九组 fixture、两个 public method、两个 internal call、不变量、错误码、枚举和专项 validator 已冻结；机器状态为 `integrated + active`。
- C++/SQLite：两个稳定 endpoint、九 Store read snapshot、固定 Clock、keyset cursor、三类投影和专用 42 天 summary 已接入；Storage 仍为 v5，无 Calendar 表或 writer。构建后 `excellent_calendar_check` 11/11 通过。
- Kotlin/JNI：窄 Contract/Handler/Bridge/JNI、production 注册、三 ABI 和安全 seeded instrumentation runner 已落地；unit、lint、Debug 主/测试 APK、ABI 符号及既有只读真机 smoke 通过。
- Flutter：真实 CalendarPage、typed adapter/controller、月周导航、三类圆点/卡片/分页、缓存刷新、创建/详情路由、Material 3 视觉、动效、reduce-motion、360dp/200%/深浅色与 Semantics 已实现；全量 535 项、analyze 和 Debug APK 通过。
- 独立 Review：后续多轮 Review 报告的 completed-series、Calendar→Habit 时区、Reminder recovery、全天提醒、默认时间、错误映射和布局问题均已返修并通过独立复验；最终仅保留正式签名/商店及设备恢复矩阵。

2026-09-02 产品负责人明确接受正式签名/商店上传、时区/DST、TalkBack、200% 字体/减少动画、强杀恢复、升级回滚/签名密钥恢复和正式 Release UI 全链为非阻断发布债。`calendar.*` 已切换为 `integrated + active`，本计划与四份分层计划及 Review 归档；签名门禁已 fail-closed 并以一次性非生产密钥验证，但尚无生产密钥/商店正式产物，剩余工作统一见 `OPEN-CAL-001`。
