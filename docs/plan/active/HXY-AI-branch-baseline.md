# HXY-AI 分支基线

本分支用于 AI 开发与实践，以 `c66066285266334939c75844c620a4b9eb0dd6ee` 为起点，在此基础上剔除本轮云同步改动。

## 保留

- `c660662` 的 Flutter UI、习惯页面美化、外观修复、Skill 与 AGENTS.md。
- 已有日程、提醒、纪念日、习惯、日历与搜索，以及 Native v2 / SQLite v5 生产链路和历史迁移。
- 原有账号登录、个人资料、头像、后端框架；未将账号模块整体删除。
- runtime.initialize 的 Storage v5 校准及回归校验，不因其与同步提交混合而回退。

## 剔除

- 本轮 Sync v1 协议、Native v3 与 Backend Sync v1 草案、workspace 协议、fixtures、冻结工具和同步验证器。
- 规划中的 SQLite v6（生产本地存储仍为 v5，未改动用户数据库）。
- C++、Kotlin、Flutter 的 Sync v1 专用组件、测试与构建入口。
- 云同步 active 计划、Sync ADR 与 Sync v1 领域模型，更新文档导航与状态。

早期未实现的同步占位与兼容墓碑保留，避免改变已有本地/账号接口；它们不是可用同步功能。历史 Git 提交和 docs/log.md 保留，不重写历史；本次清理针对当前分支文件内容。

## 后续约束

AI 功能范围与验收标准由后续任务确定；本分支不自动推进云同步计划。界面与 Skill 以 c660662 最终状态为准，不恢复该提交已撤销的字体扩展。

## 验证

实际检查结果追加在 docs/log.md。历史设备记录仅证明对应历史版本，不代表本次分支已通过设备验收。
