# State Management

负责客户端页面状态和跨页面状态。

## 具体任务

- 管理当前选中日期、日历视图模式、筛选条件、搜索关键字和搜索结果。
- 管理日程表单状态、习惯打卡状态、AI 导入候选状态。
- 管理 loading、错误、空状态、展开/折叠状态。
- 为 Presentation Layer 提供稳定的数据模型。

## 交付标准

- 状态变化应可追踪、可测试。
- 不包含 SQLite、JNI、Android SDK 调用。
- 不把复杂领域算法写在状态对象里。
