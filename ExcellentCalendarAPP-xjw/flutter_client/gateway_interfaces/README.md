# Dart Gateway Interfaces

定义 Dart 层调用底层能力的接口契约。

## 具体任务

- 定义日程、习惯、提醒、搜索、同步、AI 导入、通知、附件等接口。
- 定义跨层 DTO，保持字段语义稳定。
- 约定错误码、异常类型和调用结果格式。

## 交付标准

- 接口只描述能力，不绑定具体实现。
- 字段命名应和核心对象一致。
- 变更接口前需要同步更新 Boundary Adapter 文档和调用方。
