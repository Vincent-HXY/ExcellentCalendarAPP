version: 2

errors:
  NATIVE_INTERNAL_ERROR:
    module: common
    message: "Native internal error"
    retryable: false

  CONTRACT_VALIDATION_FAILED:
    module: common
    message: "Request does not match contract schema"
    retryable: false

  CONTRACT_VERSION_UNSUPPORTED:
    module: common
    message: "Contract version is unsupported"
    retryable: false

  FEATURE_NOT_IMPLEMENTED:
    module: common
    message: "Requested feature is not implemented in this phase"
    retryable: false

  STORAGE_NOT_INITIALIZED:
    module: storage
    message: "Native storage has not been initialized"
    retryable: false

  STORAGE_PATH_INVALID:
    module: storage
    message: "Storage path is invalid or not writable"
    retryable: false

  STORAGE_IO_ERROR:
    module: storage
    message: "Storage input/output operation failed"
    retryable: true

  STORAGE_DATA_CORRUPTED:
    module: storage
    message: "Storage data is corrupted"
    retryable: false

  TIMEZONE_ID_INVALID:
    module: runtime
    message: "Timezone ID is not present in the bundled IANA database"
    retryable: false

  TIMEZONE_DATABASE_UNAVAILABLE:
    module: runtime
    message: "Bundled timezone database is missing, corrupted, or has the wrong version"
    retryable: false

  EVENT_TITLE_EMPTY:
    module: event
    message: "Event title cannot be empty"
    retryable: false

  EVENT_TIME_INVALID:
    module: event
    message: "Event start time must be earlier than end time"
    retryable: false

  EVENT_NOT_FOUND:
    module: event
    message: "Event not found"
    retryable: false

  EVENT_DELETE_SCOPE_INVALID:
    module: event
    message: "Event delete scope is invalid"
    retryable: false

  RECURRENCE_RULE_INVALID:
    module: recurrence
    message: "Recurrence rule is invalid"
    retryable: false

  RECURRENCE_TARGET_INVALID:
    module: recurrence
    message: "Recurrence target is invalid"
    retryable: false

  ALL_DAY_RECURRING_REMINDER_NOT_SUPPORTED:
    module: recurrence
    message: "Recurring all-day Events do not support Reminder templates in Contract v2"
    retryable: false

  OCCURRENCE_NOT_FOUND:
    module: recurrence
    message: "Occurrence does not exist in the requested recurrence revision"
    retryable: false

  OCCURRENCE_OPERATION_INVALID:
    module: recurrence
    message: "Occurrence state transition is invalid"
    retryable: false

  RECURRENCE_REVISION_CONFLICT:
    module: recurrence
    message: "Expected recurrence revision does not match the Event current revision"
    retryable: false

  REMINDER_TIME_INVALID:
    module: reminder
    message: "Reminder time is invalid"
    retryable: false

  REMINDER_TARGET_NOT_FOUND:
    module: reminder
    message: "Reminder target does not exist"
    retryable: false

  REMINDER_NOT_FOUND:
    module: reminder
    message: "Reminder not found"
    retryable: false

  REMINDER_METHOD_INVALID:
    module: reminder
    message: "Reminder method is invalid"
    retryable: false

  REMINDER_IDEMPOTENCY_CONFLICT:
    module: reminder
    message: "Deterministic Reminder ID already exists with different business content"
    retryable: false

  REMINDER_SCHEDULE_CONFLICT:
    module: reminder
    message: "Reminder schedule changed before the Android registration acknowledgement"
    retryable: true

  REMINDER_SNOOZE_NOT_ALLOWED:
    module: reminder
    message: "The source ring delivery cannot create a snoozed Reminder"
    retryable: false

  DELIVERY_ATTEMPT_INVALID:
    module: notification
    message: "Delivery attempt is missing, not prepared, or finalized with a conflicting outcome"
    retryable: false

  RECOVERY_BATCH_CONFLICT:
    module: reminder
    message: "Another incomplete recovery batch conflicts with this request"
    retryable: true

  NOTIFICATION_DELIVERY_FAILED:
    module: notification
    message: "Notification delivery failed"
    retryable: true

  NOTIFICATION_INITIALIZATION_FAILED:
    module: notification
    message: "Notification system initialization failed"
    retryable: true

  HABIT_TITLE_EMPTY:
    module: habit
    message: "Habit title cannot be empty"
    retryable: false

  HABIT_NOT_FOUND:
    module: habit
    message: "Habit not found"
    retryable: false

  HABIT_CHECK_IN_DUPLICATED:
    module: habit
    message: "Habit check-in already exists for this date"
    retryable: false

  CATEGORY_NAME_EMPTY:
    module: category
    message: "Category name cannot be empty"
    retryable: false

  CATEGORY_NOT_FOUND:
    module: category
    message: "Category not found"
    retryable: false

  CATEGORY_SORT_ORDER_EXHAUSTED:
    module: category
    message: "Category sort order has reached the maximum safe integer"
    retryable: false

  ANNIVERSARY_TITLE_EMPTY:
    module: anniversary
    message: "Anniversary title cannot be empty"
    retryable: false

  ANNIVERSARY_DATE_INVALID:
    module: anniversary
    message: "Anniversary date is invalid"
    retryable: false

  ANNIVERSARY_CALENDAR_UNSUPPORTED:
    module: anniversary
    message: "Anniversary calendar type is not supported in the current version"
    retryable: false

  ANNIVERSARY_NOT_FOUND:
    module: anniversary
    message: "Anniversary not found"
    retryable: false

  ANNIVERSARY_UPDATE_CONFLICT:
    module: anniversary
    message: "Anniversary changed after the caller loaded it"
    retryable: false
    boundaries: [native, domain, storage]
    data_saved_allowed: false

  ANNIVERSARY_TARGET_DELETED:
    module: anniversary
    message: "Anniversary target is deleted"
    retryable: false
    boundaries: [native, notification_tap]
    data_saved_allowed: false

  ANNIVERSARY_REMINDER_CONFIG_INVALID:
    module: anniversary
    message: "Anniversary reminder configuration is invalid"
    retryable: false
    boundaries: [native, domain, storage]
    data_saved_allowed: false

  ANNIVERSARY_REMINDER_TEMPLATE_DUPLICATE:
    module: anniversary
    message: "Anniversary reminder templates contain a duplicate identity tuple"
    retryable: false
    boundaries: [native, domain]
    data_saved_allowed: false

  ANNIVERSARY_REMINDER_TEMPLATE_LIMIT_EXCEEDED:
    module: anniversary
    message: "Anniversary reminder plan contains more than five templates"
    retryable: false
    boundaries: [native, domain]
    data_saved_allowed: false

  ANNIVERSARY_OCCURRENCE_RANGE_INVALID:
    module: anniversary
    message: "Anniversary occurrence date range is empty or reversed"
    retryable: false
    boundaries: [native, domain]
    data_saved_allowed: false

  ANNIVERSARY_OCCURRENCE_RANGE_TOO_LARGE:
    module: anniversary
    message: "Anniversary occurrence date range exceeds 400 natural days"
    retryable: false
    boundaries: [native, domain]
    data_saved_allowed: false

  ANNIVERSARY_OCCURRENCE_FILTER_INVALID:
    module: anniversary
    message: "Anniversary occurrence filter contains a duplicate or unsupported value"
    retryable: false
    boundaries: [native, domain]
    data_saved_allowed: false

  ANNIVERSARY_OCCURRENCE_CURSOR_INVALID:
    module: anniversary
    message: "Anniversary occurrence cursor is malformed"
    retryable: false
    boundaries: [native]
    data_saved_allowed: false

  ANNIVERSARY_OCCURRENCE_CURSOR_EXPIRED:
    module: anniversary
    message: "Anniversary occurrence cursor is expired or does not match the query snapshot"
    retryable: true
    boundaries: [native, domain]
    data_saved_allowed: false

  ANNIVERSARY_OCCURRENCE_STALE:
    module: anniversary
    message: "Anniversary occurrence identity no longer matches the current target facts"
    retryable: false
    boundaries: [native, domain, delivery]
    data_saved_allowed: false

  ANNIVERSARY_REMINDER_OCCURRENCE_EXPIRED:
    module: anniversary
    message: "Anniversary reminder is past the occurrence-day catch-up deadline"
    retryable: false
    boundaries: [domain, delivery, recovery]
    data_saved_allowed: false

  ANNIVERSARY_AGGREGATE_MEMBERSHIP_CONFLICT:
    module: notification
    message: "Anniversary aggregate delivery does not match the frozen RecoveryBatch membership"
    retryable: false
    boundaries: [native, domain, delivery, recovery]
    data_saved_allowed: false

  CALENDAR_WORKFLOW_COMMIT_FAILED:
    module: storage
    message: "Calendar workflow logical commit failed before becoming authoritative"
    retryable: true
    boundaries: [storage, native]
    data_saved_allowed: false

  CALENDAR_WORKFLOW_RECOVERY_FAILED:
    module: storage
    message: "Calendar workflow recovery could not prove a complete authoritative state"
    retryable: false
    boundaries: [storage, runtime_initialize]
    data_saved_allowed: false

  SCHEDULER_RECONCILIATION_PENDING:
    module: android
    message: "Business data is saved but Android scheduling requires later reconciliation"
    retryable: true
    boundaries: [method_channel, android_scheduler]
    data_saved_allowed: true

  SEARCH_QUERY_INVALID:
    module: search
    message: "Search query is invalid"
    retryable: false

  AI_EXTRACTION_FAILED:
    module: ai
    message: "AI extraction failed"
    retryable: true

  SYNC_CONFLICT:
    module: sync
    message: "Sync conflict detected"
    retryable: false

  SYNC_OPERATION_INVALID:
    module: sync
    message: "Sync operation is invalid"
    retryable: false

  API_VALIDATION_FAILED:
    module: api
    message: "Backend API request does not match the contract"
    retryable: false

  API_UNAUTHENTICATED:
    module: api
    message: "Authentication is required"
    retryable: false

  API_FORBIDDEN:
    module: api
    message: "The authenticated user is not allowed to perform this operation"
    retryable: false

  API_RATE_LIMITED:
    module: api
    message: "Too many requests"
    retryable: true

  API_INTERNAL_ERROR:
    module: api
    message: "Backend service failed to process the request"
    retryable: true

  AUTH_INVALID_CREDENTIALS:
    module: auth
    message: "The email or password is incorrect"
    retryable: false

  AUTH_EMAIL_UNVERIFIED:
    module: auth
    message: "The login email has not been verified"
    retryable: false

  AUTH_ACCOUNT_DISABLED:
    module: auth
    message: "The account is disabled"
    retryable: false

  AUTH_ACCOUNT_NOT_FOUND:
    module: auth
    message: "The referenced account does not exist"
    retryable: false

  AUTH_ACCOUNT_ALREADY_VERIFIED:
    module: auth
    message: "The account email is already verified"
    retryable: false

  AUTH_EMAIL_ALREADY_EXISTS:
    module: auth
    message: "The email is already associated with an account"
    retryable: false

  AUTH_USERNAME_ALREADY_EXISTS:
    module: auth
    message: "The username is already in use"
    retryable: false

  AUTH_VERIFICATION_INVALID:
    module: auth
    message: "The verification credential is invalid"
    retryable: false

  AUTH_VERIFICATION_EXPIRED:
    module: auth
    message: "The verification challenge has expired"
    retryable: false

  AUTH_VERIFICATION_USED:
    module: auth
    message: "The verification challenge has already been used"
    retryable: false

  AUTH_VERIFICATION_ATTEMPTS_EXCEEDED:
    module: auth
    message: "The verification challenge exceeded the maximum failed attempts and was invalidated"
    retryable: false

  AUTH_PASSWORD_POLICY_VIOLATION:
    module: auth
    message: "The password does not meet the active password policy"
    retryable: false

  AUTH_CURRENT_PASSWORD_INVALID:
    module: auth
    message: "The current password is incorrect"
    retryable: false

  AUTH_PASSWORD_UNCHANGED:
    module: auth
    message: "The new password must differ from the current password"
    retryable: false

  AUTH_REFRESH_TOKEN_INVALID:
    module: auth
    message: "The refresh token is invalid or revoked"
    retryable: false

  AUTH_REFRESH_TOKEN_REUSED:
    module: auth
    message: "A consumed refresh token was reused and the session family was revoked"
    retryable: false

  AUTH_SESSION_EXPIRED:
    module: auth
    message: "The authenticated session has expired"
    retryable: false

  USER_PROFILE_INVALID:
    module: user
    message: "The user profile is invalid"
    retryable: false

  AVATAR_TYPE_UNSUPPORTED:
    module: user
    message: "The avatar file type is unsupported"
    retryable: false

  AVATAR_TOO_LARGE:
    module: user
    message: "The avatar file exceeds the 5 MiB limit"
    retryable: false

  AVATAR_UPLOAD_FAILED:
    module: user
    message: "The avatar could not be stored or processed"
    retryable: true

  SECURE_TOKEN_NOT_FOUND:
    module: android
    message: "No Refresh Token record exists in Android secure storage"
    retryable: false

  SECURE_TOKEN_STORAGE_FAILED:
    module: android
    message: "Android secure storage could not persist or delete the Refresh Token"
    retryable: true

  SECURE_TOKEN_CORRUPTED:
    module: android
    message: "The encrypted Refresh Token record cannot be decoded safely"
    retryable: false

  PERMISSION_DENIED:
    module: android
    message: "Required Android permission is denied"
    retryable: true

  ALARM_SCHEDULE_FAILED:
    module: android
    message: "Failed to schedule alarm"
    retryable: true

  ALARM_CANCEL_FAILED:
    module: android
    message: "Failed to cancel alarm"
    retryable: true

  NOTIFICATION_PERMISSION_DENIED:
    module: notification
    message: "Notification permission is denied"
    retryable: true

  EXACT_ALARM_PERMISSION_DENIED:
    module: reminder
    message: "Exact alarm permission is denied"
    retryable: true

  RING_CAPABILITY_UNAVAILABLE:
    module: ring
    message: "Required Android capability for ring reminders is unavailable"
    retryable: true

  RING_SETTINGS_CONFLICT:
    module: ring
    message: "Ring settings changed before the requested update"
    retryable: true

  RING_SETTINGS_STORAGE_FAILED:
    module: ring
    message: "Device-local ring settings could not be persisted"
    retryable: true

  RING_SETTINGS_CORRUPTED:
    module: ring
    message: "Device-local ring settings cannot be decoded safely"
    retryable: false

  RING_SESSION_NOT_FOUND:
    module: ring
    message: "No matching active ring session exists"
    retryable: false

  RING_SESSION_CONFLICT:
    module: ring
    message: "Active ring session changed before the requested action"
    retryable: true

  RING_ITEM_NOT_FOUND:
    module: ring
    message: "No matching item exists in the active ring session"
    retryable: false

  RINGTONE_PICKER_FAILED:
    module: ring
    message: "Android system ringtone picker failed"
    retryable: true

  RING_CONTROL_NOTIFICATION_FAILED:
    module: ring
    message: "Ring foreground control notification could not be shown"
    retryable: true

  RING_OUTPUT_UNAVAILABLE:
    module: ring
    message: "Neither ring sound nor vibration could be started"
    retryable: false

  UNSUPPORTED_REMINDER_METHOD:
    module: reminder
    message: "Reminder method is not supported in current version"
    retryable: false

  REMINDER_ALREADY_CONSUMED:
    module: reminder
    message: "Reminder has already been consumed"
    retryable: false

  REMINDER_NOT_DUE:
    module: reminder
    message: "Reminder is not due yet"
    retryable: true

  REMINDER_NOT_DELIVERABLE:
    module: reminder
    message: "Reminder is not deliverable"
    retryable: false
