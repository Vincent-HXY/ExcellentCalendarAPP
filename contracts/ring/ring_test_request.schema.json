{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://excellent-calendar.local/contracts/ring/ring_test_request.schema.json",
  "title": "RingTestRequest",
  "description": "Starts or stops a settings-page output test. A test never creates Reminder, Notification, or ActiveRingSession records.",
  "x-contract-version": 2,
  "type": "object",
  "additionalProperties": false,
  "required": ["action", "expected_settings_revision"],
  "properties": {
    "action": {"type": "string", "enum": ["start", "stop"]},
    "expected_settings_revision": {"type": "integer", "minimum": 0, "maximum": 9007199254740991}
  }
}
