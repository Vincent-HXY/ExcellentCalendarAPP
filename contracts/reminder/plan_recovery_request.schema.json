{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://excellent-calendar.local/contracts/reminder/plan_recovery_request.schema.json",
  "title": "PlanRecoveryRequest",
  "description": "Idempotent recovery request. Current time is supplied by the C++ Clock. timezone is the current device IANA timezone used only for Anniversary occurrence-day cutoffs and Reminder reprojection; ordinary 72-hour and Ring rules remain UTC-instant based.",
  "x-contract-version": 2,
  "type": "object",
  "additionalProperties": false,
  "required": ["recovery_request_id", "trigger_source", "timezone"],
  "properties": {
    "recovery_request_id": {"type": "string", "format": "uuid"},
    "trigger_source": {
      "type": "string",
      "enum": ["app_start", "device_boot", "alarm_reconcile"]
    },
    "timezone": {"type": "string", "minLength": 1}
  }
}
