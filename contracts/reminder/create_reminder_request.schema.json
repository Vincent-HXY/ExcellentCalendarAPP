{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://excellent-calendar.local/contracts/reminder/create_reminder_request.schema.json",
  "title": "CreateReminderRequest",
  "description": "Request payload for independently creating a reminder. target_id is required because Reminder must be associated through target_type + target_id.",
  "x-contract-version": 1,
  "type": "object",
  "additionalProperties": false,
  "required": [
    "target_type",
    "target_id",
    "methods",
    "is_enabled",
    "source"
  ],
  "properties": {
    "target_type": {
      "type": "string",
      "enum": [
        "event",
        "habit",
        "anniversary"
      ]
    },
    "target_id": {
      "type": "string",
      "minLength": 1
    },
    "remind_at": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "advance_minutes": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 0
    },
    "methods": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "ring",
          "popup",
          "wechat"
        ]
      }
    },
    "message": {
      "type": [
        "string",
        "null"
      ]
    },
    "is_enabled": {
      "type": "boolean",
      "default": true
    },
    "source": {
      "type": "string",
      "enum": [
        "manual",
        "auto",
        "ai_extraction",
        "sync",
        "import",
        "wechat"
      ]
    }
  },
  "anyOf": [
    {
      "properties": {
        "remind_at": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "remind_at"
      ]
    },
    {
      "properties": {
        "advance_minutes": {
          "type": "integer",
          "minimum": 0
        }
      },
      "required": [
        "advance_minutes"
      ]
    }
  ]
}
